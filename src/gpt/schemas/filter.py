schema = {
    "type": "object",
    "properties": {
        "reason": {
            "type": "string"
        },
        "is_matched": {
            "type": "boolean"
        }
    },
    "required": ["reason", "is_matched"],
}
